apiVersion: "networking.k8s.io/v1"
kind: "NetworkPolicy"
metadata:
  name: {{ include "servicea.networkpolicy.name" . }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "servicea.networkpolicy.labels" . }}
  policyTypes:
  - Ingress